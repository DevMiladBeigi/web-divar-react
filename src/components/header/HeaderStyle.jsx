import styled from "styled-components";

const Box = styled.div`
  width: 100%;
  height: 64px;
  display: flex;
  position: fixed;
  justify-content: right;
  /* @media (max-width: 1024px) {
        width: 1024px;
        height: 64px;

      } */
`;

export default Box;
